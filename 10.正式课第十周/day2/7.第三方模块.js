// npm： 开源的第三方模块管理库；
// npm 的命令是当下载node时，内置到电脑中，可以使用npm去下载模块；
// npm 服务器存储了开发常用的模块；
// npm  init -y; 初始化配置文件package.json;
// npm  install vue  --save  安装生产依赖
// npm  install  babel   --save-dev 安装到开发依赖
// npm  uninstall vue  卸载模块
// npm  install vue-cli -g; 全局安装；安装到电脑的C盘；
// npm install nrm -g : 全局安装nrm模块；
// nrm ls : 查看当前可用的源；
// nrm use taobao： 切换源
// npm install yarn -g：为了更快的下载第三方模块；
// yarn add + 模块名称；
// webpack.config.js : 是运行在node环境中；
let HtmlWebpack = require("html-webpack-plugin");
module.exports = {

}
